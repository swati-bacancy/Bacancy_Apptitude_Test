<%= form_with(model: preferred_position, local: true) do |form| %>
  <% if preferred_position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(preferred_position.errors.count, "error") %> prohibited this preferred_position from being saved:</h2>

      <ul>
      <% preferred_position.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
        <%= form.label :name %>
        <%= form.text_field :name, class: "form-control", placeholder: 'Enter Question Discription', tabindex: 1, autofocus: true %>
    <%= form.submit %>
  </div>
<% end %>
